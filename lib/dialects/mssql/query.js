var Utils          = require("../../utils")
  , AbstractQuery  = require('../abstract/query')

var Query = module.exports = function(client, sequelize, callee, options) {

}

Utils.inherit(Query, AbstractQuery)
